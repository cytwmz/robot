window.LOOI_RUNTIME_CONFIG = {
  // Keep bundled deployments on the same origin as the local backend.
  // Replace with the deployed backend origin only for a split deployment.
  backendBaseUrl: ""
};
